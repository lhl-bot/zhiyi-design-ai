# 2026-07-02

## 上线任务看板接入
- 创建 `src/components/LaunchBoardView.tsx`，可视化展示 7 项上线任务（时间线 + 状态卡片 + 进度条）
- 新增 `launchboard` 视图到 ViewType，侧边栏添加「上线进度」入口（ClipboardList 图标）
- 新增 CSS 样式 `.launch-board` 系列，沿用 Aesop 温暖大地色系
- `npx tsc --noEmit` 通过，零类型错误

## 后端 Excel/PDF 导出实现
- 安装 exceljs + puppeteer 依赖
- 创建 `server/export-excel.mjs`：设计评审汇总/详情/成本三 sheet + 工艺单 Excel，带品牌色系样式
- 创建 `server/export-pdf.mjs`：puppeteer 渲染 HTML → A4 PDF（工艺单横版、评审包竖版）
- `server/erp-readonly.mjs` 新增路由：
  - `POST /api/export/excel` → .xlsx 下载
  - `POST /api/export/pdf` → .pdf 下载
- 前端 `export.ts` 新增 `downloadExcel()` / `downloadPDF()` 函数
- App.tsx UI 更新：挑款区新增 Excel/PDF 按钮，款式卡内 Tech Pack 改为 Excel/PDF 工艺单

## 项目质量重构
- 新建 `src/constants.ts`：提取 SCORE_THRESHOLD=86、品类/面料/辅料/工艺正则、storage key、默认配置等
- 全局替换魔术数字：App.tsx / generation.ts / ComparisonView.tsx 中所有 `>= 86` → `>= SCORE_THRESHOLD`
- 删除 generation.ts 中两处人工延迟（750ms + 500ms）
- 改进评分算法：从纯模运算 → 基于品类匹配度+创意强度+数据成熟度的加权模型
- 拆分 App.tsx：提取 SidebarView → `src/components/SidebarView.tsx`，提取 TrendSection → `src/components/TrendSection.tsx`
- 清理 App.tsx 未使用导入（ClipboardList, LayoutDashboard, Palette, Users 归入 SidebarView）
- 修复 XSS：export.ts 新增 escapeHtml()，PDF 模板中 look.title / customer.name / revisionAdvice / image.src 已转义
- App.tsx 从 2166 行缩减至 1620 行（-546 行，-25%）
- 新建 `src/utils/helpers.ts`：集中 reviewStatusOf / statusTone / imageStatusMeta / copyText 等工具函数
- 提取 ProfileCard（248行）→ `src/components/ProfileCard.tsx`
- 提取 LookCard（157行）→ `src/components/LookCard.tsx`
- helpers.ts 新增 srcClass / safeHost / intelSourceLabel
- App.tsx 从 2166 → 1620 → 1190 → **487 行**（-77%）

- 错误恢复：AdvancedPanel 删除操作损毁 App.tsx，已完全重建（含所有 hook + handler + JSX）
- 修复 ErrorBoundary：移除内联 style → CSS 类
- **ERP 生产对接**：新增  接口 + ProductionPanel 组件
  - 面料库：查询 Dom_PurRequirements_Fabric（TOP 10 按用量排序，含成分/门幅/克重/历史价/供应商）
  - 辅料库：查询 Dom_PurRequirements_Accessories
  - 合同参考：近2年合同金额/交期
  - 在画像和出款之间展示，设计师可实时看到该客户能用什么面料、辅料、什么价位
