# 2026-06-30

## 后端 API 修复
- 发现前端 Vite (5173) 和后端 API (8787) 是分开启动的两个服务
- 启动了 `server/erp-readonly.mjs` 后端（需要 `--env-file=.env` 加载环境变量）
- 连接 ERP 数据库正常（123.57.4.44:2531/RichMaxGarmentBF_ssmj）
- 配置了 DEEPSEEK_API_KEY (sk-2652...)，AI 品牌智能分析可用
- ARK_API_KEY 已配置，火山方舟 Doubao 出图可用

## 前端 Bug 修复
- 款式卡片删除按钮（×）不生效 → `.look` 缺少 `position: relative` 导致绝对定位按钮锚点错误，加了即可
- `minLookCount` 从 4 改为 1，可以只生成 1 款

## SVG 矢量技术线稿系统
- 创建了 `src/services/techSketch.ts` — 完整的 SVG 矢量技术线稿生成器（5种品类）
- 支持夹克/外套、连衣裙、裤装、上装、马甲，含领型/口袋/门襟/省道/缝线等工艺细节
- 线稿图模式不再调用火山方舟生图 API

## 位图 → 矢量转换 (imagetracerjs)
- 安装了 imagetracerjs，实现 raster → SVG 矢量化
- `src/services/vectorize.ts`：加载图片 → Canvas → imagetracer trace → 输出 SVG 路径
- 在款式卡片添加「转矢量」按钮，点击即可将火山方舟生成的线稿转为可编辑 SVG
- 类型声明文件 `src/types/imagetracerjs.d.ts`
- TypeScript 编译通过
